apply plugin: 'java'

defaultTasks 'jar'

//Define the contents of the jar file
jar  {
    destinationDir = file(".")
    archiveName = 'resources.jar'
}

dependencies {
    compile files('lib/resources.jar')
}